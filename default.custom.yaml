patch:
    ascii_composer/switch_key:
        Shift_L: noop
        Shift_R: inline_ascii
        Control_L: noop
        Control_R: noop
    key_binder/bindings:
        - { when: always, accept: Shift+space, toggle: noop }
        - { when: always, accept: Shift_R+space, toggle: full_shape }
